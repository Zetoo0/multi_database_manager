/*use crate::repository::postgres_repository::PostgresRepository;
use crate::repository::mssql_repository::MsSqlRepository;
use crate::repository::mysql_repository::MySqlRepository;
use crate::repository::sqlite_repository::SqLiteRepository;
use crate::repository::oracle_repository::OracleRepository;
pub enum RepositoryType{
    Postgres(Box<dyn PostgresRepository>),
    Mssql(Box<dyn MsSqlRepository>),    
    MySql(Box<dyn MySqlRepository>),
    SqLite(Box<dyn SqliteRepository>),
    Oracle(Box<dyn OracleRepository>)
}*/