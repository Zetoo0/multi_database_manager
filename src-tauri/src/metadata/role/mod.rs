pub struct Role{
    pub name: String,
    pub is_super: bool,
    pub is_insherit: bool,
    pub is_create_role: bool,
    pub is_create_db: bool,
    pub can_login: bool,
    pub is_replication: bool,
    
}