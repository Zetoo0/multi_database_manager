csatlakozás összerakása --?
adatok elérése az adatbázisból(all view,table,column,stored procedures stb) --
createhez cuccok adatbázisonként --?
sql highli

init database on connect -> Load all the listings for the given driver -> return them(Vec<Database>,RolesAndUsers)
structs:
Database:
    Catalog(if any)
    Event Trigger(if any)
    Foreign Data Wrapper(if any)
    Schemas:
        Aggregates(if any)
        Foreign Tables(if any)
        Functions
        Materalized Views
        Sequences
            Vec<Seq>
        Procedures
            Vec<Procedure>
        Tables
        Trigger Functions
        Types
        Views


